import React from 'react';
import Nav from './nav';
import Footer from './footer';

const Layout = () => {
    return (
        <div>
        <Nav/>

            
        </div>
    );
}

export default Layout;
